# 1. 프로그램 설계서 (5점)

## 1. 프로젝트 주제
선수 기록 관리 프로그램

## 2. 핵심 기능 목록
(구현할 기능들을 순서대로 나열하세요.)
1. 선수 정보 입력
2. 선수 목록 출력
3. 선수 검색 기능
4. 통계 기능 (최고 득점자 / 평균 득점 계산)

## 3. 데이터 구조 설계
// 선수 정보를 저장하기 위한 구조체 정의
struct Player {
    char name[30];     // 선수 이름
    char position[20]; // 포지션 (예: 공격수, 수비수 등)
    int goals;         // 득점 수
    int games;         // 출전 경기 수
};


**방법 1: 배열 사용 (기본)**
char names[100][30];     // 선수 이름 저장
char positions[100][20]; // 포지션(공격수, 수비수 등) 저장
int goals[100];          // 득점 수 저장
int games[100];          // 출전 경기 수 저장
int count = 0;           // 현재 등록된 선수 수


**방법 2: 구조체 사용 (선택)**
// 선수 정보를 저장하기 위한 구조체 정의
struct Player {
    char name[30];     // 선수 이름
    char position[20]; // 포지션 (공격수, 수비수 등)
    int goals;         // 득점 수
    int games;         // 출전 경기 수
};

